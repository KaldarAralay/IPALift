<?xml version="1.0" encoding="utf-8"?>
<!-- Replace APP-EVIDENCE-ID in Adapter.cpp with the app's traceability entry. -->
<{{APP_SLUG}}Slice>
  <startup>
    <initialRoute>start</initialRoute>
    <nextRoute>main</nextRoute>
    <title>{{APP_NAME}}</title>
  </startup>
</{{APP_SLUG}}Slice>
